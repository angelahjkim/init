# Find the command that kills the background command's process
# If you just ran the background process

sudo pkill tail

# otherwise you need to find the PID using the 'ps' command to get the PID
# kill [PID]