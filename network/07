# Query an external DNS server on the slash16.org domain name (ie. google 8.8.8.8)

nslookup slash16.org 8.8.8.8

# 8.8.8.8 is the primary DNS server for Google -- so we are getting 
# domain information about slash16.org through Google